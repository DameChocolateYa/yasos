import <standard>
use {len, scani};

gsize = (msg: str) -> int:
	let size: int = msg.len;
endfn

start = ():
	"Ponga algo: ";
	let msg: str = scani();
	let size: int = get msg.len;

	"El tamaño es: ", size, ln;

    if (size lte 10) then {
        "Es corto", ln;
    }
    if (size gt 10) then {
        if (size lte 20) then {
            "Es mediano", ln;
        }
    }
    if (size gt 20) then {
        "Es largo", ln;
    }
endfn

start<>;

end();
