import <standard>
use {scani, strcmp};

start = ():
    while (true) do {
        let msg: str = scani();
        if (strcmp(msg, "stop")) then {
            stop;
        }
    }
endfn

start<>;

end();
