import <standard>
use {scani, stoint};

func = (msg: str, n: int):
	let i: int = 1;
	while (i lte n) do {
		"", i, ".", msg, cr;
		inc i;
	}
	unload i;
endfn

start = ():
	"Msg: ";
	let msg: str = scani();
	"N: ";
	let n: int = stoint(scani());

	func<msg: msg, n: n>;
endfn

start<>;

end();